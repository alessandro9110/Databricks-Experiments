# This is a Databricks asset bundle definition for synthia_data_agent.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: synthia_data_agent
  uuid: 00b2c081-b946-4eb6-86ce-c56549a14945

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-879327577164218.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/alessandro.armillotta@mitavanadeitaly.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      host: https://adb-879327577164218.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/alessandro.armillotta@mitavanadeitaly.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/alessandro.armillotta@mitavanadeitaly.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: alessandro.armillotta@mitavanadeitaly.onmicrosoft.com
        level: CAN_MANAGE
